func LabelWidth(n float64) float64 {
	xs := value.NewArray[float64](1, 2, 3)
	ys := value.MapArray[float64, value.BStr](xs, func(x float64) value.BStr {
		return value.NumberToString((x * n))
	})
	total := 0.0
	for _, y := range ys.Elems() {
		total += y.Length()
	}
	return total
}
